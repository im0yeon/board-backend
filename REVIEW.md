# 리뷰 지침

## 리뷰 언어

모든 리뷰 코멘트는 한국어로 작성한다. 기술 용어는 영어를 그대로 쓰되, 처음 등장할 때 `token (토큰)` 형식으로 한국어를 병기한다.

## Important의 기준

이 저장소에서 🔴 Important는 다음으로 한정한다.

- 로직 오류, 경계 조건 누락, NPE 가능성
- 트랜잭션 경계 오류, 영속성 계층의 stale 데이터 (아래 "항상 확인할 것" 참고)
- Flyway 마이그레이션과 JPA 엔티티 불일치로 부팅이 깨지는 변경
- 로그·예외 메시지에 개인정보나 요청 본문이 노출되는 경우
- 인증·권한 우회

네이밍, 구조 취향, 리팩터링 제안, 주석 규칙 위반은 최대 Nit이다.

## 항상 확인할 것

이 프로젝트의 영속성 계층은 리포지토리 인터페이스 1개에 구현체 1개를 두고, 구현체 내부에서 **쓰기·단건조회는 Spring Data JPA, 목록·검색·통계는 MyBatis Mapper**로 나눠 호출하는 병행 구조다. 여기서 반복적으로 사고가 나므로 다음을 매번 검사한다.

- JPA 쓰기 결과에 의존해 MyBatis로 조회하면서 그 사이 flush가 보장되지 않는 코드. JPA 변경 감지는 `flush()` 전까지 MyBatis 조회에 보이지 않는다. 다만 `saveAndFlush()`나 명시적 `flush()` 호출, IDENTITY 전략의 INSERT처럼 반영이 이미 보장된 경로는 지적하지 않는다. 같은 트랜잭션이라는 사실만으로는 근거가 되지 않는다.
- Y/N 플래그 컬럼은 반드시 `CHAR(1)`이어야 한다. `VARCHAR(1)`이면 Hibernate `YesNoConverter`와 `ddl-auto: validate`가 충돌해 부팅이 실패한다. MyBatis 조회 SQL에서는 `(col = 'Y') AS ...` 변환이 있는지 확인한다.
- 스키마 소유자는 Flyway다. 엔티티 필드가 추가·변경됐는데 `src/main/resources/data/flyway/` 마이그레이션이 함께 오지 않으면 Important로 보고한다. 기존 마이그레이션 파일을 수정하는 변경도 Important다.
- `data/` 하위 패키지를 옮기는 변경은 `application.yaml`의 `mybatis.type-aliases-package`와 `@MapperScan` 경로를 함께 고쳐야 한다.
- 서비스 계층에 JPA·MyBatis 타입이 노출되면 지적한다. 페이징은 `Pageable`이 아니라 자체 `PageCommand` / `PageResponse`를 쓴다.
- 컨트롤러가 엔티티를 그대로 반환하지 않는지 확인한다.

## 주석과 로그

주석은 **생략이 기본**이다. 코드가 이미 설명하고 있으면 주석이 없는 쪽이 낫다. 다음은 발견 시 Nit으로 보고한다.

- AI 생성 표시 (`// Generated by Claude`, `// AI-generated` 등) — 주석과 로그 양쪽 모두 금지
- 변경 이력 주석 (`// 2026-01-15 수정`, `// FIXED`, 작성자명)
- 자명한 설명 (`// getter 메서드`, `# 서버 포트`), 변수·메서드명을 그대로 반복하는 주석
- 기술 스택 나열 (`// MyBatis 동적 쿼리`)

로그 메시지는 한국어로 쓰고 변수값에 컨텍스트를 붙인다. `log.info("성공")`처럼 맥락 없는 메시지는 Nit이다.

## Nit 개수 제한

한 리뷰당 Nit은 최대 5개까지만 인라인으로 남긴다. 더 있으면 요약에 "그 외 유사 항목 N건"으로 적는다. 발견한 것이 전부 Nit이면 요약을 "차단 이슈 없음"으로 시작한다.

## 보고하지 않을 것

- `build/`, `.gradle/`, `gradle/wrapper/` 하위 파일
- 포맷·들여쓰기·import 정렬 등 CI 린터가 잡는 항목
- 테스트 코드가 프로덕션 규칙을 의도적으로 위반하는 부분
- 커밋 메시지 컨벤션 (별도 도구가 담당한다)

## 재리뷰

이미 한 번 리뷰한 PR에는 새로운 Nit을 추가하지 않는다. Important만 보고한다.
